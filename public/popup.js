// This file is intentionally left blank or can be removed.
// The primary popup logic is now intended for dashboard.js,
// if manifest.json's default_popup is set to dashboard.html.

// If popup.html is still used as an intermediary or loading screen,
// you might add logic here, for example:
// window.location.href = 'dashboard.html'; // to redirect immediately
// Or display a loading message.
console.log("popup.js loaded - should be minimal if dashboard.html is the main popup.");
